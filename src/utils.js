export const backendURL = "https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/all_day.geojson";

export const initialState = {
    earthquakeData: {},
    loading: false,
    error: null,
  };

export const delay = 500;